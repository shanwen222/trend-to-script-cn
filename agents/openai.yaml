interface:
  display_name: "热点到成稿"
  short_description: "自动准备依赖，把实时热点变成经审核的平台文案"
  default_prompt: "Use $trend-to-script-cn to prepare its required dependencies and turn current topics into audited, platform-adapted copy for my industry."

policy:
  allow_implicit_invocation: true
